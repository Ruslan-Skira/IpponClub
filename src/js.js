class Calculator {
    constructor() {
        this.operatorsList = ['+', '-', '/', '*'];
        this.operands = {
            this.currentOperand = [],
            this.currentOperator = [],
        }

    }

}




let calculator = new Calculator();

console.log(calculator.calculate(16 / 4));